// Названия полей сохранённых данных в асинхронном хранилище

export enum ENKS {
    adressServer = 'adressServer',
    locale = 'locale',
    userAuth = 'userAuth',
    userAuthData = 'userAuthData',
    userLinks = 'userLinks',
    userProfile = 'userProfile',
    fcmToken = 'fcmToken'
}